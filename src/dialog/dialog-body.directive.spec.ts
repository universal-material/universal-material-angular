import { DialogBodyDirective } from './dialog-body.directive';

describe('DialogBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogBodyDirective();
    expect(directive).toBeTruthy();
  });
});
