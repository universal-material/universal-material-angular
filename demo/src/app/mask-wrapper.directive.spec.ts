import { MaskWrapperDirective } from './mask-wrapper.directive';

describe('MaskWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
